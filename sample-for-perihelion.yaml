---
name: Talos Boot Assets Generation

# yamllint disable-line rule:truthy
on:
  schedule:
    - cron: 0 0 * * *
  workflow_dispatch: { }

env:
  REGISTRY: ghcr.io
  # renovate: depName=ghcr.io/siderolabs/qemu-guest-agent
  QEMU_GUEST_AGENT_VERSION: 8.1.0
  # renovate: depName=ghcr.io/siderolabs/i915-ucode
  I915_UCODE_VERSION: 20230310
  # renovate: depName=ghcr.io/siderolabs/iscsi-tools
  ISCSI_TOOLS_VERSION: v0.1.3

stuff:
  and:
    - things
